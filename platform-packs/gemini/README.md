# Gemini 安装包

这一套文件用于把 `Photo Pose Director` 配成：

- Gemini Gem
- Gemini 系统指令 / 粘贴式长期使用版本

## 目录说明

- `gem/`：给 Gemini Gem 使用
- `instructions/`：给普通 Gemini 使用

## 推荐使用方式

如果你能创建 Gem，优先使用 `gem/`。

如果只是普通 Gemini 会话，可把 `instructions/system-prompt.md` 作为长期主提示词使用。
